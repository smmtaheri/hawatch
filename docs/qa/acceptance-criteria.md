# معیارهای پذیرش milestone handoff

## repository

- [ ] مسیر repository `/home/nobitex/Desktop/Tasks/Nobitex/hawatch-product` است.
- [ ] ساختار monorepo مورد توافق وجود دارد.
- [ ] `apps/web`، `apps/api`، `infra` و `scripts` فقط placeholder دارند.
- [ ] هیچ package manager file، dependency، app code، migration، model یا API اجرایی اضافه نشده است.
- [ ] repository Sites تغییر نکرده است.

## تصاویر

- [ ] هر ۱۶ تصویر اصلی در `design/source-screens` حفظ شده است.
- [ ] برای هر تصویر نسخهٔ مرتب‌شده در `design/screens` موجود است.
- [ ] width، height، page، theme، device و sha256 در manifest ثبت شده است.
- [ ] تصاویر resize، compress یا re-encode نشده‌اند.

## مستندات

- [ ] README، AGENTS و .env.example موجودند.
- [ ] tokenها و design system مستند شده‌اند.
- [ ] هر چهار صفحه همهٔ ۱۴ بخش specification را دارند.
- [ ] flowها، page behavior، API contract، معماری و ADRها موجودند.
- [ ] ابهام‌ها و تصمیم‌های باز صریح ثبت شده‌اند.

## خروجی نهایی

- [ ] `git status` بررسی شده است.
- [ ] remote ساخته، push، build، test یا deploy انجام نشده است.
- [ ] هیچ تغییر بیرونی روی منابع مرجع انجام نشده است.

