"""Retired schema namespace.

The public catalog has only points and routes.  This module intentionally has
no active model; its migration package remains so existing databases can be
upgraded in order before the legacy table is removed.
"""
