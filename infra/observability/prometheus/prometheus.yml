global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: hawatch-api
    metrics_path: /api/v1/metrics/
    scheme: http
    bearer_token_file: /run/secrets/hawatch_metrics_token
    static_configs:
      - targets: [api:8000]
